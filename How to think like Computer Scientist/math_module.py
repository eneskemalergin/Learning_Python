# The Math Module #

''' Calling math module '''
import math
# Now we called that module for using in our code.

print math.pi # shows pi

print math.e  # show the value of e

print math.sqrt(2.0) # takes float as a paramaters and returns the square root value

print math.sin(math.radians(90))
